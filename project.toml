[project]
name = "email-listener"
version = "0.1.0"

[run]
command = "bash -c 'export JAVA_HOME=$(dirname $(dirname $(readlink -f $(which java)))) && mvn spring-boot:run'"

[env]
AZURE_CLIENT_ID = ""
AZURE_CLIENT_SECRET = ""
AZURE_TENANT_ID = ""
